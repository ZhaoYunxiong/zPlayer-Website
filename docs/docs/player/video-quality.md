# 画质与滤镜：先稳定，再追求效果

画质选项适合在“视频已经能稳定播放”之后使用。着色器、滤镜和其他实时处理会增加 GPU 或 CPU 负担，画面变漂亮的同时，也可能带来卡顿、发热或音画不同步。

## 播放中可以快速处理的画面问题

在播放器菜单的“视频”分组中，可以按当前片源处理：

- **视频轨道**：多轨视频时选择需要播放的轨道；
- **画面比例**：在默认、16:9、16:10、4:3 等比例之间切换；
- **旋转**：适合手机拍摄或方向错误的素材；
- **水平翻转、垂直翻转**：处理镜像或倒置画面；
- **场景浏览器**：通过视频缩略图快速查看和定位场景。

这些调整主要作用于当前播放体验，不会自动修改原始视频文件。

## 着色器和滤镜的区别

着色器可以组合使用，滤镜通常选择一个。它们属于实时画面处理，适合有明确目标时再开启，例如改善缩放观感、处理特定显示效果或尝试补帧。

如果开启后出现以下现象，先关闭滤镜和着色器，再重新播放同一片源：

- 播放进度不连续或明显掉帧；
- 风扇转速、GPU/CPU 占用突然升高；
- 音画逐渐不同步；
- 画面闪烁、撕裂或颜色异常。

如果关闭后恢复正常，说明更可能是实时处理负载或兼容性问题，而不是文件损坏。

## 长视频更适合用时间轴和场景浏览

只是想找一个画面，不必反复拖动进度条。可以先把鼠标移到进度条上查看缩略图；需要反复回看的剧情，则在[内容结构](/docs/player/content-modules)中建立章节书签。两者的区别是：缩略图帮助你临时找位置，章节书签会留下可以再次使用的内容标记。

## 画质调整建议

1. 先确认默认渲染稳定。
2. 只调整画面比例、旋转或翻转等不会明显增加计算量的选项。
3. 再逐个试着色器或滤镜，每次只开启一个组合。
4. 用同一片源对比开启前后的 CPU、GPU、掉帧和音画同步。
5. 网络片源画质不理想时，先更换线路或确认服务端码率，滤镜无法补回原本不存在的细节。

## 动漫画质：从轻量预设开始

动漫线条、色块和低分辨率放大对着色器比较敏感。下面是适合第一次尝试的方向，不是固定答案；同一部番在不同显卡、片源和屏幕上，最舒服的组合可能不同。

| 你的片源或目标 | 可以先试 | 说明 |
| --- | --- | --- |
| 老番、480p 或较糊的 720p | `AniSD` 或 `Anime4K (LQ)` | 先改善低清线条，负担相对小 |
| 普通 720p / 1080p 动漫 | `Ani4Kv2` 或 `Anime4K (MQ)` | 二选一开始比较，避免一上来叠加多个重型放大器 |
| 高性能台式机，愿意换取更高负载 | `Anime4K (HQ)` | 画面处理更重，先确认 GPU 占用、掉帧和音画同步 |
| 需要放大但不确定用哪种动漫预设 | `FSRCNNX`、`RAVU`、`NNEDI3` 或 `AMD-FSR (EASU)` | 更偏通用的缩放/边缘处理，适合逐个比较 |
| 想让线条更锐利、压低部分糊感 | `AdaptiveSharpen (RT)` | 适合轻度锐化；过强时可能出现边缘光晕 |

简单记忆即可：标清老番先从 `AniSD` / `Anime4K (LQ)` 开始，普通高清先试 `Ani4Kv2` / `Anime4K (MQ)`，高性能设备再考虑 `Anime4K (HQ)`。同一时间先启用一个主要放大预设，比较前后效果后再决定是否叠加其他着色器。

## 动漫场景下的滤镜推荐

滤镜更偏向补帧或高成本的实时处理，和着色器不是一回事。播放器内置的三个方向可以这样理解：

| 滤镜 | 适合谁 | 建议 |
| --- | --- | --- |
| `MVTools（LQ）` | 大多数想尝试补帧的电脑 | 负担相对低，建议作为第一次补帧测试 |
| `RIFE（DML）` | 显卡性能较好、确实想要更明显补帧效果 | 负载明显更高，先用短片段确认稳定性 |
| `UAI（DML）` | 高端设备和非常明确的超分需求 | 成本极高，不建议和 `Anime4K (HQ)` 一起作为第一次组合 |

一个稳妥的试法是：先只开动漫着色器，确认原始播放流畅；再关闭着色器测试 `MVTools（LQ）`；最后才尝试 `RIFE（DML）` 或 `UAI（DML）`。着色器可以组合，但重型超分、锐化和补帧同时开启很容易让负载叠加。

如果出现掉帧、风扇突然加速、音画不同步或画面异常，先关闭滤镜，再逐个恢复。着色器和滤镜只处理实时显示，不会把处理后的结果写回视频文件；网络片源本身码率不足时，也不能凭空恢复细节。
